'use client';

import React from 'react';
import styles from './FirstFloorPage.module.css'; // Import the CSS module

const FirstFloorPage = () => {
  return (
    <div className={styles['first-floor']}>
      <p>First floor would be here soon.</p>
    </div>
  );
};

export default FirstFloorPage;
